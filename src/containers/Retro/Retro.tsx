function Retro() {
  // const { id } = useParams<{ id: string }>();
  // const [isNavbarOpen, setIsNavbarOpen] = useState(false);

  return (
    <div>
      <h1>Retro</h1>
    </div>
  );
}

export default Retro;
